# 更新日志

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循语义化版本。

## [Unreleased] — 开源发布

项目更名为 **zero-dep-likes** 并公开发布。**功能代码零改动。**

### 变更

- **项目更名**：`blog-likes` → `zero-dep-likes`，文档中的示例目录名同步更新。
- **文档脱敏**：示例域名统一为 `example.com` / `blog.example.com` /
  `staging.example.com`；数据库占位符统一为 `your_database_name` /
  `your_database_user` / `your_database_password`。
- **`$SALT` 的默认值**从"看起来像真值的随机串"改成显眼的
  `CHANGE_ME_PLEASE_REPLACE_WITH_A_LONG_RANDOM_STRING`。
  默认值最怕被直接抄进生产环境，占位符会逼着你换掉它。
- 新增 `.gitignore`（排除 `config.local.php` 和日志）、`LICENSE`（MIT）、
  `docs/preview.svg`（README 预览图）。
- `README.md` 重写开头，面向第一次接触本项目的读者。

### 说明

这一版**没有修改任何功能代码**——`likes.php` / `db.php` / `response.php` /
`likes.js` / `likes.css` 的逻辑与 v1.8.0 完全一致，只是配置默认值和文案变了。

---

## [1.8.0] — 2026-09-11

**新增取消点赞。** 要重传 `api/likes.php`、`api/response.php`、`assets/likes/likes.js`、
`assets/likes/likes.css`、`check.html`。

### 为什么加

用户提出：误点了赞之后没法撤回，"别人误点了，想取消点赞就不行了"。
这是真实的使用缺陷——点赞按钮在中文语境里几乎都支持取消。

### 新增

- **取消点赞接口**：

  ```http
  POST /api/likes.php
  { "article_id": "…", "visitor_id": "…", "action": "unlike" }
  ```

  返回 `{"code":0,"data":{"like_count":…,"liked":false,"removed":true}}`。

  - 不带 `action` 时默认仍是点赞，**老调用方不用改**（向后兼容）
  - 同时接受 `DELETE` 方法作为别名，方便用工具直接调
  - **没赞过就取消不算错误**，返回 `code:0` 且 `removed:false`（幂等）

- **前端改为可切换**：已赞状态下按钮**不再禁用**，再点一次即取消。

  - 鼠标悬停有 title 提示「点击取消点赞」
  - `aria-label` 在「点赞这篇文章」/「取消点赞」之间切换
  - 取消成功提示「已取消点赞」

- **400ms 防误触**：因为已赞状态仍可点，双击会变成"点赞 → 立刻又取消"，
  用户只看到数字跳一下又回去。现在 400ms 内的第二次点击被忽略，
  想真心取消过一会儿再点即可，不影响正常使用。

- **`check.html` 新增三项**（⑨ 取消点赞 / ⑩ 取消后可以重新点赞 / ⑪ 没赞过时取消），
  原来的 ⑨~⑬ 顺移为 ⑫~⑯，现在共 **15 项自动检查 + 1 项跳过**。

- `tests/api-test.md` 新增第 6 章「取消点赞」五个子用例；
  验收标准对照表补第 11 条。原 6~13 章顺移为 7~14。

### 关键实现决策

- **先删明细，再减计数。** 这样"计数 = 明细条数"这个不变量始终成立。
  反过来做的话，一旦删明细失败，就会出现"计数少了 1 但那个人仍被记为已赞过"——
  他既赞不了也取消不掉，只能手工改数据库。
- **计数减 1 外面套 `GREATEST(..., 0)`**，任何异常情况下都不会变成负数。
- **取消点赞不计入限流**：它不新增明细，没有刷的价值；把取消也限流的话，
  正常用户误点两次反而会被拦。
- **取消用 `POST + action` 而不是 `DELETE`**：部分虚拟主机 / CDN / WAF 会拦掉
  带请求体的 `DELETE`，症状是"本地 curl 好用、线上莫名失败"，极难排查。
  `DELETE` 只作别名。
- **`api/response.php` 的 `Access-Control-Allow-Methods` 加上 `DELETE`**。

### 验证

用 jsdom 把 `likes.js` 灌进真实页面跑了完整的来回测试，**14 项断言全过**：

| 检查 | 结果 |
|---|---|
| 初始状态（计数 10、未赞） | ✓ |
| 点一下 → 计数 11、aria-pressed=true、文字变「已赞」、按钮仍可点 | ✓ |
| 隔 500ms 再点 → 计数 10、回到未赞 | ✓ |
| 再点 → 又能点赞（可反复切换） | ✓ |
| 间隔 50ms 连点两次 → 只生效一次（防误触） | ✓ |

另外把四个页面（`check.html` / `demo.html` / `live-test.html` / `icon-preview.html`）
的内联脚本抽出来过了 `node --check`，全部通过。

---

## [1.7.3] — 2026-09-11

缓存破除。**代码逻辑没动，只给示例页的脚本引入加了 `?v=` 查询串。**

### 背景

`live-test.html` 上按钮始终不出现，用户已多次重传文件并强刷浏览器。

**先证明代码没问题**：用 jsdom 把 `likes.js` 真跑了一遍（这是本次排查的关键转折——
前面几轮全靠读代码猜，全猜错了），四个用例全部通过：

| 用例 | 结果 |
|---|---|
| 空组件 `<div class="like-widget"></div>` | 按钮自动生成，计数正常 |
| 空组件 + 显式 `data-article-id` | 正常 |
| 完整结构（应保持不变） | 未被改动 |
| 空组件 + 编辑器注入的属性 | 正常 |

真实页面 `examples/live-test.html` 的端到端测试也通过：按钮生成、计数渲染、
版本横幅打印、派生 ID 写入 DOM。

**所以是缓存。** 而且很可能不只是浏览器缓存：**国内虚拟主机常有 CDN / 静态资源缓存层**，
那种情况下缓存不在用户电脑上，**强刷浏览器完全无效**，只有换 URL 才能绕过。

### 修改

- `examples/live-test.html` 与 `examples/demo.html` 的脚本引入加上 `?v=1.7.3`。
- `TUTORIAL.md` 第 4 章第三步把 `?v=` 从"建议"改为**强烈建议**，
  并说明原因（浏览器缓存 + 主机/CDN 缓存，后者强刷无效）。
  同时写明如何用控制台的版本横幅确认当前跑的是哪一版。

> **为什么不改代码逻辑**：jsdom 已验证逻辑正确，问题不在代码。改代码只会掩盖真正的原因。

---

## [1.7.2] — 2026-09-11

统一配置项的占位符措辞。**改的是文案与默认值，逻辑没动。**

### 修改

- `api/config.php` 与 `api/config.local.example.php` 的数据库三项占位符统一为
  `your_database_name` / `your_database_user` / `your_database_password`。

  **刻意留成一眼看得出「还没填」的占位符**，而不是看起来像真值的默认值：
  如果默认值长得像真的，忘了改时会报出令人困惑的连接失败；
  占位符则会把问题直接指向「这里没填」。

- `TUTORIAL.md` / `DEPLOY.md` 同步更新数据库配置的说明。

---

## [1.7.1] — 2026-09-11

`where.php` 增加"解压多套了一层目录"的自动探测。**只改这一个文件。**

### 背景

用户反馈"明明每次都重新上传解压，服务器上的 `likes.js` 还是旧版"。
排查发现她打开的那个 `likes.js` 头部写着 `版本: v1.0.0` —— 是 v1.4.0 或更早的代码，
所以按钮才一直不出现。

最可能的成因：**压缩包里的顶层文件夹被一起解压出来了**，
新文件全落进了 `/zero-dep-likes/zero-dep-likes/`，而外层那些旧文件原封不动，
于是"传了但没生效"。

### 新增

- **`where.php` 自动探测嵌套目录**：扫描同层子目录，如果某个子目录里
  也有 `api/likes.php`，就判定为"解压多套了一层"，并打印：

  - 哪个子目录里有另一份 `api/`
  - 可以直接验证的地址（如 `…/zero-dep-likes/api/likes.php?article_id=test-001`）
  - 怎么修（把内层的 `api/` 和 `assets/` 移到外层覆盖旧文件，再删掉空目录）
  - 提醒解压时选「解压到当前目录」，不要把压缩包里的 `zero-dep-likes/` 那一层也解出来

  没有嵌套时，明确告知"解压位置没问题"，并提示那大概率是浏览器缓存（`Ctrl+F5`）。

  这一条本可以避免前面好几轮来回 —— 一个专门查路径问题的工具，
  应该能识别这个最常见的成因。

---

## [1.7.0] — 2026-09-11

让"服务器上跑的到底是哪一版"变成一行就能确认的事。
**要重传 `assets/likes/likes.js`。**

### 背景

排查过程中连续卡在同一个问题上：**分不清"文件没传上去"还是"浏览器用了缓存的旧文件"**。
两者的现象一模一样（同样的旧报错、同样的失效行为），但处理方式完全不同，
每次都要靠猜，白费好几轮。

### 新增

- **支持 `api/config.local.php` 本地配置覆盖**，为"每次升级重传整个压缩包"的工作流设计。

  问题：升级时把压缩包整个解压，`config.php` 会被覆盖回模板状态 —— 数据库密码丢了，
  每次都得重填，而每次重填都是一次填错的机会（这个坑已经真实发生过一次 500）。

  现在 `config.php` 会在最后加载同目录的 `config.local.php`（如果存在），
  **它覆盖任何一项配置**。把数据库密码、salt、白名单放进这个文件，
  它就永远不会被解压覆盖，一次配好、长期有效。

  附带 `api/config.local.example.php` 作为填空模板，含必填三项和可选调整项。
  文件不存在时行为与之前完全一致，不影响任何现有部署。

- **`likes.js` 加载时在控制台自报版本号和实际 API 地址**：

  ```
  [likes] likes.js v1.7.0 已加载 · API: /zero-dep-likes/api/likes.php
  ```

  有这一行 = 服务器上的脚本确实是新的。
  没有这一行 = 服务器文件是旧的，**或者**浏览器缓存了旧文件。

  同时把 `VERSION` 提成脚本里的一个常量，改脚本时顺手改它。

- **组件结构不对时，在页面上也给提示**，不再只写控制台。
  以前的表现是"页面上什么都没有"，用户根本不知道该去开控制台；
  现在会在组件位置留一行字：`点赞组件结构不对，已跳过（详见浏览器控制台）`。

### 文档

- `TUTORIAL.md` 第 3.3 节改为**优先推荐 `config.local.php`**，
  说明如何创建、为什么不会被覆盖。
- `TUTORIAL.md` 第 4 章第三步的引入代码加上 **`?v=1.7.0` 查询串**，
  并说明它的作用：**更新脚本后强制浏览器重新下载**，不吃缓存。
  这不是必须的，但能省掉"传了新文件却感觉没生效"这类困惑。
- 补充说明：遇到"传了新文件但行为没变"，先 `Ctrl + F5` 强刷。

---

## [1.6.1] — 2026-09-11

补齐"子目录部署"这一前提，并补上安全边界说明。
**要重传 `assets/likes/likes.js` 和 `where.php`。**

### 背景

用户明确了部署前提：**虚拟主机上有多个项目，每个项目都放在各自的子目录下**。
这解释了此前连续两次 404 —— 都是"默认项目在网站根目录"导致的。
于是把项目里剩下的同类假设一并清掉。

### 修复

- **`where.php` 打印的验证地址写死了根目录**（`https://域名/api/likes.php`）。
  一个专门用来排查路径问题的工具，却给出一个必然 404 的地址，反而误导排查。
  现在按自身位置拼接，并在信息区顶部直接显示推断结果：

  ```
  推断的接口地址 : https://staging.example.com/zero-dep-likes/api/likes.php?article_id=test-001
  ```

- **`likes.js` 的用法注释用的是根目录路径**（`/assets/likes/likes.js`），
  与实际推荐部署方式不符。改为子目录形式，并注明"路径按实际部署改"。

- **兜底到默认值时会给出明确警告**。`/api/likes.php` 这个默认值假定项目在网站根目录，
  只在拿不到脚本地址时（defer / module 加载）才会用到。现在命中它会打印：
  "这个默认值假定项目部署在网站根目录，如果你的项目在子目录里请显式写 data-api"。

### 文档

- `TUTORIAL.md` 第 5 章新增「**安全确认（含一个必须纠正的误解）**」：
  - **明确说明 CORS 白名单不是安全防线** —— 它只能拦住别的网站的前端 fetch，
    拦不住 curl / 脚本 / 服务端调用。CORS 是浏览器的自律规则，服务端没有这个概念。
  - 说明"没有密钥"对匿名点赞是**固有形态而非疏漏**：没有用户身份就没有可凭据的东西，
    而且放在前端的密钥等于没有密钥。
  - 给出实际生效的防护对照表（唯一索引 / 限流 / 预处理 / 哈希 / 403 守卫 / 最小权限）。
  - 明确最坏情况是「某篇文章的数字被刷得不可信」，**不是**「系统被攻破」。
  - 上线前逐条确认清单，以及"想再紧一点"的可选手段。
- `README.md` 第五章开头同样补上这个纠正。

---

## [1.6.0] — 2026-09-11

补上「API 地址怎么定」和「别人怎么调用」两块文档，并给 API 地址加了一层自动推断。
**要重传 `assets/likes/likes.js`。文档改了三份，可选。**

### 新增

- **忘了写 `data-api` 时，可从脚本自身地址反推 API 地址。**

  以前漏写 `data-api` 会退化成写死的 `/api/likes.php`，部署在子目录时直接 404。
  现在多了一层推断：`api/` 和 `assets/` 在同一个项目目录下，层级关系固定，
  从脚本自己的位置往回推一定推得出来——

  ```
  /zero-dep-likes/assets/likes/likes.js   →   /zero-dep-likes/api/likes.php
  /assets/likes/likes.js              →   /api/likes.php
  ```

  脚本地址带查询串（`likes.js?v=1.6.0`）也能正确处理。
  解析顺序现在是四级：`data-api` 属性 → `window.LIKES_API` → 脚本地址反推 → 默认值。

### 文档

- `TUTORIAL.md` 新增 **4.4「API 地址是怎么定下来的」**：
  明确回答"`likes.js` 要不要改成我的链接"（不用，文件里没有写死任何域名），
  给出四级解析优先级表，以及三种路径写法（根相对 / 绝对 / 页面相对）的取舍——
  **推荐根相对**，因为文章 URL 层级深浅不一，页面相对会算错。
  另附多域名指向同一目录时的说明。

- `TUTORIAL.md` 新增 **4.5「别人怎么调用这个接口」**：
  给出接口清单，并重点讲清一个高频困惑——
  **CORS 只管浏览器，不管服务端**：

  | 谁在调 | 受白名单限制吗 |
  |---|---|
  | 浏览器地址栏直接打开 | 不受 |
  | 服务端（curl / Python / PHP） | 不受 |
  | 同域名 page 里的 fetch | 不受 |
  | 别的网站的前端 fetch | **受** |

  以及两个必须提醒调用方的约束：`visitor_id` 需 8–128 位可见 ASCII，
  `article_id` 只允许 `A-Za-z0-9_-`。最后说明接口地址本就是公开的，
  防护靠唯一索引 + 限流 + 最小权限，不是靠保密。

---

## [1.5.1] — 2026-09-11

**修一个会让"一行就够"直接失效的 bug。要重传 `assets/likes/likes.js`。**

### 修复

- **空组件被直接跳过，页面上什么都不显示。**

  文档和教程里承诺「模板里只要写一行 `<div class="like-widget"></div>`」，
  但 `likes.js` 实际要求组件里**已经存在** `.like-button` 和 `.like-count`——
  它只负责找，不负责建。于是空组件走到"结构不完整"分支，被静默跳过，
  只在控制台留一句警告，页面上一点痕迹都没有。

  **文档和代码对不上，是我的问题。修的是代码，不是文档** ——
  因为"一行就够"这个承诺本身是对的，它让填充模板变得几乎不可能出错。

  现在：组件里什么都没有时，自动建出按钮 + 图标 + 计数三层结构。
  已经写了完整结构的组件不会被碰。

  另外把"结构不对被跳过"的警告写得更明确：直接说明需要哪个 class、
  以及要么清空成空组件、要么按文档抄完整结构。以前那句话只说"结构不完整"，
  用户看完还是不知道该改什么。

### 文档

- `TUTORIAL.md` 第 4 章补上"为什么空的也能用"的说明，并用折叠块收纳完整写法。
- `TUTORIAL.md` 第 7 章新增症状「页面上什么都没有，连按钮都看不见」，
  按控制台输出分三种情况给出判断。
- `README.md` 第 5 步标注空组件为最省事写法。

---

## [1.5.0] — 2026-09-11 · 首个稳定版

补上完整教程文档，并明确三份文档的分工。**只新增/修改了文档，代码零改动，不用重传任何文件。**

### 稳定版依据

后端功能已通过自检页逐项验证：**12 项通过 + 1 项跳过**（跳过项为 CORS，
同源场景测不了，不影响使用）。覆盖：接口可达、配置访问守卫、数据库连通与建表、
非法参数拦截、首次点赞、**数据持久化**、**重复点赞拦截**、不同访客累加、
文章隔离、原文章未被污染、批量查询、响应头。

### 新增

- **`TUTORIAL.md`：完整教程。** 十章，约 500 行：
  1. 它是什么（含数据流示意）
  2. 确认你的环境
  3. 部署：从零到能跑（七步，每步都有「做完应该看到什么」）
  4. 接进博客（三种场景：模板变量 / 手写 HTML 自动派生 / 列表页必须逐个指定）
  5. **上线前的验收清单**（后端自动 / 前端手点 / 并发命令行，三块分开列）
  6. 日常维护（备份、常用 SQL、清理测试数据、重置单篇）
  7. **按症状查问题**（七类症状，含"查询正常但点赞 400"这类高迷惑性故障）
  8. 常见需求怎么改（换配色、用 emoji、加取消点赞、关限流、显示谁赞过）
  9. **原理与设计取舍**（数据怎么组织的、防重复三道防线、为什么 1001 返回 200、
     隐私设计、为什么 article_id 决定一切、限流为何按明细计数）
  10. 文件清单（必须上传 / 一次性工具 / 可选图标 / 不用上传 / 如何彻底卸载）

### 文档结构调整

三份文档分工明确，避免互相重复又互相矛盾：

| 文档 | 定位 |
|---|---|
| `TUTORIAL.md` | 第一次部署看。完整教程 + 原理 + 排错 |
| `DEPLOY.md` | 赶时间看。一页纸七步清单，只有操作和验证点 |
| `README.md` | 当参考手册查。配置项、API 契约、安全设计、维护 SQL |

README 顶部加了导航表，DEPLOY.md 顶部加了指向教程的说明。

---

## [1.4.0] — 2026-09-11

新增真实接口的试用页。**要重传：`assets/likes/likes.js`、`examples/live-test.html`（新增）。**

### 新增

- **`examples/live-test.html`：接真实数据库的试用页。**
  与 `demo.html` 的分工：`demo.html` 用内置假后端（不需要服务器，看长相和交互）；
  本页接你自己的 API 和 MySQL，验的是"真的能存进去、刷新还在"。

  上传到 `/zero-dep-likes/examples/` 后打开即可。页面会显示本页地址、
  组件**实际派生出**的 `article_id`，并给出搬到博客上的三步改法。

  它故意不写 `data-article-id`，用来演示「按链接自动派生」。

- **派生的 ID 现在会写回 DOM**：`likes.js` 把算出来的 `article_id`
  写进组件的 `data-article-id` 属性，并标注来源
  （`data-article-id-source="auto"` 或 `"explicit"`）。
  这样在开发者工具里一眼就能看到"这篇文章到底用了什么 ID"，不用翻控制台。

### 说明

- 没动 `api/` 下任何文件，也没动 `check.html`。

---

## [1.3.0] — 2026-09-11

支持按文章链接自动隔离，CORS 白名单支持通配子域，补齐图标的多种格式。
**要重传：`api/config.php`、`api/response.php`、`assets/likes/likes.js`、`check.html`，
以及 `assets/likes/` 下新增的几个图标文件。**

### 新增

- **组件可以不带 `data-article-id`，自动按当前页面链接派生。**
  模板里只写 `<div class="like-widget"></div>`，每篇文章自动得到自己的 ID，
  **不存在"忘了改 ID"这回事**。

  派生规则：去掉扩展名与 index → 非字母数字一律变 `-` → 折叠连续 `-` → 去首尾 `-`。

  | 链接 | article_id |
  |---|---|
  | `/posts/hello-world.html` | `posts-hello-world` |
  | `/posts/hello-world/` | `posts-hello-world`（与上一条归一，不分裂） |
  | `/文章/你好` | `p-` + 短哈希（中文路径归一化后为空，退回哈希，保证不撞） |

  超过 180 字符的路径会截断并追加短哈希；哈希用 FNV-1a 同步实现
  （`crypto.subtle` 是异步的，这里用不了）。

- **同一页的"ID 重复"检测。** 这是自动派生的已知代价：列表页上所有组件的 URL 相同，
  派生出的 ID 也相同，会共用一个计数。`likes.js` 现在会把同一页里重复的 ID 抓出来，
  在**页面上**用强调色告警，不只是写控制台。

  两种触发都会被抓到：复制模板忘改 `data-article-id`；在列表页用了自动派生。

- **CORS 白名单支持通配子域**：`'https://*.example.com'` 放行该域名的任意子域。
  匹配**要求落在点边界上**，`evilexample.com` 这类冒名域名不会被误放行
  （已用等价实现跑过 15 组边界用例，含 `example.com.evil.com`、协议不符、端口不符）。
  注意 `*.example.com` **不含裸域名本身**，`https://example.com` 要单独写一行。

- 默认白名单补上 `https://blog.example.com` 与 `https://*.example.com`。

- README 新增「站点图标」一节：**上传 `icon.svg` 不会让图标自动生效**，
  必须在页面 `<head>` 里声明 `<link rel="icon" type="image/svg+xml" href="…">`，
  每个页面各自声明一次；路径要按实际部署调整；浏览器会缓存图标的 404，需 `Ctrl + F5` 强刷。

- **补齐图标的 PNG / ICO 版本**：`icon-32.png`、`icon-180.png`、`icon-512.png`、`favicon.ico`。
  原有 `icon.svg` 保留为主用。

  为什么还要 PNG：**部分服务器的 MIME 配置不认 `image/svg+xml`**，
  会把 svg 当纯文本或 `application/octet-stream` 发出去，浏览器就不把它当图标用。
  PNG 的 MIME 类型没有这个歧义，作为兜底。

  命名沿用通用约定：`icon-180.png` 对应 `apple-touch-icon`，
  `favicon.ico` 内含 16/32/48 三种尺寸。

- **`assets/likes/icon-preview.html`：图标自检页。**
  放在 `assets/likes/` 里、与图标文件同目录，相对路径零歧义。
  它会**用 `<img>` 实际加载四个图标文件并逐个报结果** ——
  这同时验证了两件事：文件在不在，以及服务器有没有把它们当图片类型发出
  （MIME 不对时 `<img>` 同样是一张裂图）。

  能一次区分三种情况：文件没上传 / MIME 类型不对 / 文件正常但浏览器缓存了之前的 404。
  页面上还写明：**`/api/likes.php` 返回的是 JSON，不是网页，标签页永远不会给它显示图标**——
  不要用接口地址去验证 favicon。

### 修复

- **`check.html` 默认 API 地址写死成 `/api/likes.php`，部署在子目录时全部 404。**
  以前它假定自己躺在网站根目录；一旦部署成 `/zero-dep-likes/check.html`，
  所有请求都打到 `https://域名/api/likes.php`（不存在），13 项检测全军覆没，
  而且**看起来像是数据库挂了**，方向完全错。

  现在改成**按本页位置自动推断**：`本页目录 + /api/likes.php`
  （`/zero-dep-likes/check.html` → `/zero-dep-likes/api/likes.php`），并在输入框下方提示推断依据。

  另外补了一条定向提示：**当失败项全部是 HTTP 404 时**，直接说明"这个地址下根本没有文件，
  跟数据库无关，是地址不对"——避免用户被误导去查数据库。

### 说明

- 这个版本没有动 `api/likes.php` / `api/db.php`。
- 两种填 ID 的方式可以混用：**写了 `data-article-id` 就用你写的，没写才自动派生。**

---

## [1.2.0] — 2026-09-11

新增一次性建表程序 `install.php`。**这是一个新文件，`api/` 下的 PHP 一个字都没改，不用重传。**

### 新增

- **`install.php`：一次性建表程序。** 放在 `api/` 同级，浏览器打开点一下就把两张表建好，
  建完可以一键把自己删掉。相比手动导入 phpMyAdmin，省掉了"选库"这个最容易出错的环节。

  它做的事：

  - 自动定位并加载 `api/config.php`（同时探测几种常见布局），**不重复配置数据库信息**
  - 页面上先展示连接情况：库名、主机、MySQL 版本、PHP 版本、实际读到的配置文件路径
    ——**先让你确认"表要建在哪个库"，再让你点建表**
  - 建表前显示两张表的当前状态，建表后显示每张表的结果（已创建 / 跳过 / 失败）
  - **自动校验字段结构**是否与 `sql/schema.sql` 一致，不一致会列出缺了哪些、多了哪些
  - **单独校验唯一索引 `uniq_article_visitor`**——防重复点赞全靠它，少了会明确警告
  - 连不上数据库时，直接显示 MySQL 原话并附错误码对照表（1045/1044/1049/2002）
  - 建完后自动拼出**你这次部署的真实接口地址**，省得自己猜路径
  - 支持一键删除自身；删除失败（权限不足）会提示改用 FTP 手动删

  安全设计：

  - 只执行 `CREATE TABLE IF NOT EXISTS`，幂等，**没有任何 DROP / ALTER / DELETE**
  - **不会自动执行**：打开页面只读状态，必须手动点按钮才动手（爬虫误触发也没影响）
  - 不显示数据库密码，只显示库名与主机
  - 页面上多次提示用完即删

- **`assets/likes/icon.svg`：站点图标。** 珊瑚橙 `#F77234` 圆角方形 + 白色拇指块面。
  方形在标签页里比异形图标更稳；实心块面（而非描边线条）才能在 16px 下辨认得出来；
  手背竖条与手掌之间特意留了一道缝——这道缝是"拇指"能被读出来的关键，去掉就是个不明方块。
  已接进 `demo.html` / `check.html` / `install.php`。

- **前端新增"占位 ID"告警。** 点赞数完全由 `data-article-id` 决定，
  所以多篇文章填了同一个 ID 时，它们会**静默共用同一个计数**——
  不报错、不提示，只是数字莫名其妙地一起涨。这是整套设计里唯一会"安静出错"的地方。

  `likes.js` 现在会检测 ID 是不是 `post-1`、`article_id`、`test`、`xxx`、`todo` 这类
  明显的示例值，命中就在**页面上**（不只是控制台）用强调色提示出来：

  > 文章 ID 还是示例值「post-1」，会和其他文章共用点赞数，请改成这篇的 slug

  宁可误报，也不能让"多篇文章共用一个点赞数"悄悄发生。

### 修复

- `install.php` 最初用 `SHOW TABLES LIKE ?` / `SHOW COLUMNS` / `SHOW INDEX` 检查结构。
  **MySQL 的 `SHOW` 语句不支持预处理协议**，而本项目关闭了模拟预处理
  （`ATTR_EMULATE_PREPARES = false`），这会直接抛
  `This command is not supported in the prepared statement protocol yet`。
  已全部改用 `information_schema` 查询（普通 SELECT，可正常绑定参数）。

---

## [1.1.2] — 2026-09-11

排错体验改进。**改了 `api/config.php` 和 `api/db.php`、`api/likes.php` 两个文件，需要重新上传这三个。**

### 改进

- **`$DEBUG = true` 时直接回传 MySQL 的原话。** 之前数据库连不上时，
  前端只能看到 `data.debug: "database connection failed"`——知道是数据库问题，
  但不知道是密码错、库名错、还是用户没绑库，还得去翻日志。
  现在 `data.error` 里会带上完整异常：

  ```json
  {"code":500,"msg":"server error","data":{
    "error":"RuntimeException: database connection failed: SQLSTATE[HY000] [1045] Access denied for user 'abc'@'localhost' (using password: YES)",
    "where":"db.php:78"}}
  ```

  MySQL 的错误码是分门别类的（1045 密码错 / 1044 没绑库 / 1049 库名错 /
  1146 表不存在 / 2002 主机地址错），一眼能对症。
  **生产环境 `$DEBUG` 保持 `false`，对外仍只有一句 `server error`。**

- **`$ALLOWED_ORIGINS` 默认值补上 `https://staging.example.com`**（虚拟主机自带域名）。

### 修复

- **文档补上一个极难自查的陷阱：GET 正常但 POST 返回 400。**
  浏览器规范里**同源 GET 不发 `Origin` 头，同源 POST 会发**。
  所以只要正在访问的域名没写进白名单，症状就是"点赞数读得到，一点赞就 400"——
  半死不活，很难联想到 CORS。已在 `config.php` 注释、README 专条、
  DEPLOY.md 第 3 步三处前置警告。

---

## [1.1.1] — 2026-09-11

新增定位工具。**`api/` 下的 PHP 文件一个字都没改，已部署的不用重新上传。**

### 新增

- `where.php`：一次性部署诊断文件。上传到「你以为的网站根目录」后打开，它会直接打印：
  - 本文件所在绝对路径、服务器认定的网站根目录（`DOCUMENT_ROOT`）、PHP 版本、服务器软件
  - **自动判断**当前目录是不是网站根目录（对比路径得出，不必自己猜）
  - 检查同级目录下 `api/` 和 `assets/` 各项文件是否齐全，列出缺失项
  - 列出该目录下实际存在哪些目录与文件
  - 根据以上结果给出结论和下一步动作

  用于解决 `nginx 404` 这类**文件位置不对**的问题——它只做只读检查，不写任何东西。
  **修好之后必须删除**（会暴露服务器路径）。

  > 之所以单独做这个文件，是因为 `404` 有多种成因（多套一层目录 / FTP 根目录不等于网站根目录 /
  > 域名绑定的目录是另一个），靠猜地址很费时间。上传一个文件打开一次就能定向。

---

## [1.1.0] — 2026-09-11

新增部署自检能力。**`api/` 下的 PHP 文件一个字都没改，已部署的不用重新上传。**

### 新增

- `check.html`：一键部署自检页。上传到网站根目录后打开即用，自动跑 12 项检查
  （接口可达、配置访问守卫、数据库连通与建表、非法参数拦截、首次点赞、数据持久化、
  重复点赞拦截、不同访客累加、文章隔离、批量查询、响应头），逐条给出期望值与实际值，
  并支持「复制结果」把报告贴出来。看完即删的临时文件。
- `DEPLOY.md`：一页纸上手清单。从放文件到端到端验收共 7 步，每步都写明
  「这一步做完你应该看到什么」以及常见失败对应的处理方式。

### 说明

- 自检页必须放在**自己的域名下**（同源），不能本地双击打开——
  接口有 CORS 白名单，本地 file:// 的来源是 `null`，会被拒。
- 自检页里的 CORS 检测项标记为「**跳过**」而非自动判定：`Origin` 是浏览器的
  禁止修改请求头，JS 设不进去；同源请求本来也不带它。硬测只会得到假阴性，
  反而把人引去查不存在的问题。页面上给了手动验证的 curl 命令。

---

## [1.0.0] — 2026-09-11

首个可用版本，覆盖需求规格中的全部必需项。

### 新增

**后端**
- `api/likes.php`：API 入口，支持 `GET` / `POST` / `OPTIONS`
- 查单篇：`GET ?article_id=xxx&visitor_id=yyy`
- 批量查：`GET ?article_ids=a,b,c&visitor_id=yyy`，单次上限可配，自动去重，缺失文章补 `0` 而非漏掉
- 点赞：`POST` JSON 或表单，返回最新计数
- `api/config.php`：数据库、salt、CORS 白名单、限流、日志，全部集中可配；数据库项支持环境变量覆盖
- `api/db.php`：PDO 单例连接，关闭模拟预处理，出错抛异常
- `api/response.php`：统一 `{code, msg, data}` 外壳 + CORS 白名单处理

**数据库**
- `sql/schema.sql`：`article_likes`（计数）+ `like_records`（明细防重）
- `like_records` 上 `(article_id, visitor_hash)` 唯一索引，从数据库层面锁死"一人一篇一赞"
- 脚本可重复执行，附校对 SQL 与常用维护语句

**前端**
- `assets/likes/likes.js`：纯原生 JS 零依赖，自动发现 `.like-widget`，多篇自动走批量接口，批量失败降级为逐个请求
- `assets/likes/likes.css`：颜色全部继承博客（深浅色自动适配），仅"已赞"状态使用珊瑚橙强调色

**文档与测试**
- `README.md`：五步部署、配置项逐条说明、API 文档、安全设计、排错手册、维护 SQL
- `tests/api-test.md`：13 组 curl 用例 + 验收标准对照表
- `examples/demo.html`：内置假后端的离线预览页

### 关键设计决策

- **点赞成功后返回 HTTP 200 而非错误码**：`1001`（已赞过）是业务状态不是错误，返回 200 让前端能一并读到权威计数，不用写异常分支
- **事务内读计数**：刚 UPDATE 过的事务已持有行排他锁，读到的一定是自己的最新值，不会被并发读到中间态
- **死锁重试 3 次**（退避 50ms / 100ms）：并发撞车时不丢数据
- **图标用内联 SVG 而非 `👍` emoji**：emoji 作功能图标在不同系统渲染差异大、与博客排版难对齐。想换回去见 README 第九节「自定义」
- **限流基于成功写入的明细计数**：重复点赞不产生新明细，所以正常用户几乎不可能触发，主要拦脚本批量刷不同文章

### 安全

- 全程 PDO 预处理 + 参数绑定，关闭模拟预处理；限流窗口值强制 `(int)` 后拼接
- `article_id` 白名单式校验（仅 `A-Za-z0-9_-`，1–191 字符）
- `visitor_id` 校验长度 8–128、仅可见 ASCII
- 库中不存访客原始标识，`visitor_id` / IP / User-Agent 均加盐哈希后落库
- 非入口 PHP 文件带访问守卫，直接访问返回 403
- CORS 仅回显白名单域名，拒绝时输出 `Vary: Origin`
- 生产环境错误响应不含数据库信息，细节写入日志

### 已知限制

- 访客清空浏览器 localStorage 后可重复点赞 —— 匿名方案的共同限制，接入登录体系可彻底解决
- 修改 `$SALT` 会使已有访客被识别为新访客
- 文章 slug（`article_id`）变更会丢失该文章的点赞数
- 未实现"取消点赞"，当前为点过即锁

---

## 未发布的计划

见 README 第十节「迭代路线」。优先级建议：

1. 后台管理页（查看 / 重置 / 导出）
2. 点赞趋势图表（数据已具备）
3. 登录用户绑定 `user_id`
